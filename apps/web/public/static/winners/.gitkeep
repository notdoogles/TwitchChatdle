Drop winner images here (any format: .png, .jpg/.jpeg, .gif, .webp, .avif, .svg).
One is picked at random and shown when the player wins a round.
This file just keeps the otherwise-empty directory tracked in git; it is not served or referenced by the app.
